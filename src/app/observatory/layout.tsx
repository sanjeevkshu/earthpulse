// Observatory layout — passes through to root layout (Navbar + Footer included).
// Observatory pages use standard EarthPulse theming: light/dark via site toggle.
export default function ObservatoryLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
